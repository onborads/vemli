import "../globals.css";
import "../tailwind.css";

export const metadata = {
  title: "Vemli",
  description: "Vemli POS website",
};

export default function RootLayout({ children }) {
  return children;
}
